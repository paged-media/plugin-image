# Reference checkouts — third-party material studied during incubation
# (read-only mounts; never committed, never vendored into this repo).
references/

# Rust
/target

# Node
node_modules/
dist/
*.tsbuildinfo

# Optional psd-tools ecosystem-oracle venv (test-only, machine-local)
.venv/

# wasm-bindgen build outputs (regenerated by scripts/build-wasm.sh;
# the manifest declares manifest/wasm/image_js_bg.wasm)
manifest/wasm/*.wasm
manifest/wasm/*.js
manifest/wasm/*.d.ts
